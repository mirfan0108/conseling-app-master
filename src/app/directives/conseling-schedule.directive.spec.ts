import { ConselingScheduleDirective } from './conseling-schedule.directive';

describe('ConselingScheduleDirective', () => {
  it('should create an instance', () => {
    const directive = new ConselingScheduleDirective();
    expect(directive).toBeTruthy();
  });
});
